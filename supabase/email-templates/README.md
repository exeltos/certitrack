# CertiTrack Supabase Auth email templates

These are source-controlled reference templates for deployment. They are not automatically applied by `db push`.
Configure them in Supabase Auth during the controlled deployment phase.

Required templates:
- confirm-signup.html
- reset-password.html
- invite-user.html

Keep links based on Supabase template variables; never hard-code tokens.
