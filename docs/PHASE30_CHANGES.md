# Phase 30 — Global Record Actions

- Standardized record action rail at the far right of every certificate/partner row.
- Certificate actions: View (in-app only), Edit + optional PDF replacement, partner visibility toggle, Delete.
- Removed download actions from certificate rows, including partner certificates.
- Partner-owned shared certificates are view-only.
- Visibility toggle uses `is_private` and controls whether active partners can see each certificate.
- Demo mirrors the same action order and replacement/visibility behaviors.
- CSS cache version bumped to v30.
